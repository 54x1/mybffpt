# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

old_bk

node_modules
dist
dist-ssr
*.local

# Env files (may contain secrets/config, never commit)
.env
.env.*
!.env.example
.npmrc

# Encrypted exports (user data, never commit)
*.enc

# Test output
coverage
playwright-report
test-results
playwright/.cache

# PDF diagnostic harness output — contains real bank-statement transactions,
# never commit (the tests/pdfdiag.test.ts harness itself is safe to keep).
pdfdiag-out.txt
tests/pdfdiag-report.txt
.frugal*

# Vite cache
.vite/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Generated SBOM (built in CI and uploaded as an artifact; regenerate with npm run sbom)
sbom*.json

tests/pdfdiag-*.txt
pdfdiag-*.txt
